FROM amazoncorretto:25 AS build
WORKDIR /workspace
COPY . .
RUN ./gradlew :auth-data-plane:bootJar --no-daemon

FROM amazoncorretto:25
WORKDIR /app
COPY --from=build /workspace/auth-data-plane/build/libs/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
