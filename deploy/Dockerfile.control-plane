FROM amazoncorretto:25 AS build
WORKDIR /workspace
COPY . .
RUN ./gradlew :auth-control-plane:bootJar --no-daemon

FROM amazoncorretto:25
WORKDIR /app
COPY --from=build /workspace/auth-control-plane/build/libs/*.jar app.jar
EXPOSE 8081
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
